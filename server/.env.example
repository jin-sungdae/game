# Copy to .env (ignored), set a local secret, and export before bootRun/compose.
LUMA_DB_PASSWORD=
# Optional overrides:
# LUMA_DB_URL=jdbc:postgresql://127.0.0.1:55432/luma_game
# LUMA_DB_USER=luma
# LUMA_SERVER_PORT=8081

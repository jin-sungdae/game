Authoritative Companion behavior lives in src-tauri/src/companion/ (controller, explicit transition rules, weighted selection and local master configuration). src-tauri/src/behaviors.rs composes it with the existing PIP lifecycle. One Rust clock serves all windows. Rendering receives state, position and direction; no React timers, random decisions or duplicate simulation.
